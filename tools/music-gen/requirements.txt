# Music Generation Service Dependencies
# Install into a dedicated venv to avoid conflicts with the backend or ComfyUI

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
aiohttp>=3.9.0

# Audio
soundfile>=0.12.0
numpy>=1.24.0

# ---------------------------------------------------------------------------
# Engine-specific install instructions
# ---------------------------------------------------------------------------
#
# ACE-Step 1.5 (primary engine for this workstation — 8 GB VRAM, Windows)
# - Official portable Windows package: https://files.acemusic.ai/acemusic/win/ACE-Step-1.5.7z
# - Or clone + uv sync:
#     git clone https://github.com/ace-step/ACE-Step-1.5.git
#     cd ACE-Step-1.5
#     uv sync
# - Does NOT install via pip; clone the repo or use the portable package.
#
# Default model: ACE-Step/acestep-v15-sft (2B DiT + 0.6B LM, Tier 3)
# Override with ACESTEP_MODEL_ID env var if needed.
#
# GPU monitoring (optional)
# gpustat
# pynvml
