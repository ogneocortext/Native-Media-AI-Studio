/**
 * Local timing primitives for StillIRise V7.
 * Re-exports the canonical types and helpers from `shared/timing.ts`.
 *
 * If you need a Remotion-only addition, add it here and upstream to
 * `shared/timing.ts` when it is generally useful.
 */

export * from "../../../../shared/timing";
